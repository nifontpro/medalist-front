import Spinner from '@/ui/Spinner/Spinner';

const LoadingDepartmentId = () => {
  return (
    <main>
      <Spinner />
      <div>Loading Department Id...</div>
    </main>
  );
};

export default LoadingDepartmentId;
